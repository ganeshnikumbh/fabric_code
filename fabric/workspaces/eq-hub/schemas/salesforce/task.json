{
  "table_name": "task",
  "description": "Salesforce Task object — one row per Task (activity) record from the SOQL query. The API returns paginated page envelopes (totalSize/done/nextRecordsUrl/records); records are extracted from each page's 'records' array and flattened. attributes.type/url carry the SObject metadata; ActivityDate is an ISO date string stored as-is in bronze. WhoId/WhatId are the polymorphic relationship ids.",
  "source_path": "records",
  "context_fields": ["start_date", "end_date", "record_type", "source"],
  "fields": [
    {"column": "attributes_type", "source": "attributes.type", "type": "string"},
    {"column": "attributes_url",  "source": "attributes.url",  "type": "string"},
    {"column": "id",              "source": "Id",              "type": "string"},
    {"column": "subject",         "source": "Subject",         "type": "string"},
    {"column": "status",          "source": "Status",          "type": "string"},
    {"column": "priority",        "source": "Priority",        "type": "string"},
    {"column": "activity_date",   "source": "ActivityDate",    "type": "string"},
    {"column": "description",     "source": "Description",     "type": "string"},
    {"column": "owner_id",        "source": "OwnerId",         "type": "string"},
    {"column": "who_id",          "source": "WhoId",           "type": "string"},
    {"column": "what_id",         "source": "WhatId",          "type": "string"}
  ]
}
